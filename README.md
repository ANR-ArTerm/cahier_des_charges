## Architecture des dossiers

- `CDC_version_1` correspond à la première version du cahier des charges
- `exemples_donnees` contient des données pour le développement
	- les index (personnes et lieux)
	- un texte (agucchi) et les notices d'œuvre JSON associées
	- un dossier `exemples_donnees` contenant :
		- les formats des notices JSON en fonction des types (artwork, building, ensemble)

